      <div class="col-sm-8">
        <dl class="dl-horizontal">
          <dt>User</dt>
          <dd><%= review.user.firstName %></dd>
          <dt>Description</dt>
          <dd><%= review.description %></dd>
          <dt>Star Rating</dt>
          <dd><%= review.rating %></dd>
        </dl>
              <% if review.user_id == current_user.id %>
            <%= button_to class: 'btn btn-xs btn-danger' %>
          <% end %>
      </div>
    
    </div>